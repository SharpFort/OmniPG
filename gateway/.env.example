# ==============================================================================
# OmniPG 网关环境配置
# 用法：复制此文件为 .env 并修改变量值
# cp .env.example .env
# ==============================================================================

# ------------------------------------------------------------------------------
# PostgreSQL (通过 host.docker.internal 访问 WSL2)
# ------------------------------------------------------------------------------
DB_HOST=host.docker.internal
DB_PORT=5432
DB_USER=app_owner
DB_PASSWORD=dev_password_change_me
DB_NAME=app_db

# ------------------------------------------------------------------------------
# Pgbouncer
# ------------------------------------------------------------------------------
PG_BOUNCER_PORT=6432
AUTHENTICATOR_PASSWORD=authenticator_dev_pass

# ------------------------------------------------------------------------------
# APISIX
# ------------------------------------------------------------------------------
APISIX_HTTP_PORT=9080
APISIX_HTTPS_PORT=9443
APISIX_ADMIN_PORT=9180
# ⚠️ 安全警告: 默认值与 APISIX 官方示例相同（全网公开），
#    任何非本地环境部署前必须更换为随机值（如: openssl rand -hex 16）
APISIX_ADMIN_KEY=edd1c9f034335f136f87ad84b625c8f1

# ------------------------------------------------------------------------------
# PostgREST
# ------------------------------------------------------------------------------
PGRST_PORT=3001

# JWT Secret
JWKS_JSON={"keys":[{"kty":"oct","kid":"dev-hs256","alg":"HS256","k":"c2VjcmV0X2RldmVsb3BtZW50X2tleV9hdF9sZWFzdF9zZXZlbl9jaGFyYWN0ZXJzIQ=="}]}


# ------------------------------------------------------------------------------
# Swagger UI
# ------------------------------------------------------------------------------
SWAGGER_PORT=8082

# ------------------------------------------------------------------------------
# Logto Webhook（init-apisix-routes.sh 需要；从 Logto 控制台 Webhook 配置获取 signing key）
# ------------------------------------------------------------------------------
LOGTO_WEBHOOK_SIGNING_KEY=dev_webhook_signing_key_change_me

# ------------------------------------------------------------------------------
# Logto 数据库（与业务库同机同集群，2026-08-20 决策；compose DB_URL 引用）
# ------------------------------------------------------------------------------
LOGTO_DB_PASSWORD=logto_dev_pass_2026
