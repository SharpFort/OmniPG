# 第三轮评估 —— 逐缺口核查与剩余疑点

> **评估对象：** `结合-PostgREST-与-Casbin-权限方案 (3).md`（1310行，专家对第二轮 10 个缺口的回复）
> **目标：** 确认所有缺口是否已闭合，标注剩余零散疑点，决定是否还需要下一轮

---

## 逐个缺口核查

| # | 缺口 | 第二轮状态 | 第三轮回复 | 判定 |
|:---:|:---|:---:|:---|:---:|
| 1 | Policy Syncer 完整可执行代码 | ❌ | 完整的 Go 代码：pq.NewListener 自动重连、Sync()、Reconcile()、formatToCSV()、pushToApisix() | ✅ 闭合 |
| 2 | CSV policy 拼接格式 | ❌ | 尾部 NULL 丢弃 + 中间 NULL 保留原则、映射表、伪代码、示例输入输出 | ✅ 闭合 |
| 3 | RS256 签名方案 | ❌ | 确认 pgjwt 不支持 RS256，给出 PL/Python3u + PyJWT 完整方案 | ✅ 闭合 |
| 4 | JWKS 端点 | ❌ | APISIX mocking 插件（推荐）+ PostgREST 本地 JWKS JSON 直写（两种方案） | ✅ 闭合 |
| 5 | Syncer 并发控制 | ❌ | 单线程事件循环 + 1秒防抖归并，完整 select-channel 代码 | ✅ 闭合 |
| 6 | sys_menu 表 DDL | ❌ | 完整 7 表 DDL（含 sys_department），升级的 get_user_menu()，菜单/API 关系深度辨析 | ✅ 闭合 |
| 7 | httpOnly Cookie | ❌ | 方案A：PostgREST set_config('response.headers') / 方案B：APISIX serverless-post-function 抹除 RT | ✅ 闭合 |
| 8 | tenant_id/dept_id + JWT payload | ❌ | 更新的 sys_user DDL（含 tenant_id/dept_id/sys_department），新 JWT payload（roles 数组） | ✅ 闭合 |
| 9 | Role-in-JWT model.conf | ❌ | 完整 model.conf（regexMatch 替代 g 规则，删除 role_definition），匹配案例演练 | ✅ 闭合 |
| 10 | 视图→物化视图平滑迁移 | ❌ | 4 步零停机迁移方案（原子事务 DDL + CONCURRENTLY 刷新 + 触发器升级 + 网关热升级） | ✅ 闭合 |

### 结论：10/10 全部闭合 ✅

---

## 仍然存在的零星疑点（非阻塞，建议整理性追问）

所有结构性缺口已经封闭，以下是方案文档在整合时可能遇到的一致性细节问题：

### 疑点 1：Policy Syncer 代码中的 model.conf 未随 Role-in-JWT 更新

**问题：** 缺口 1 给出的 Go Syncer 代码中，`CasbinModelConf` 常量仍然是旧版（含 `[role_definition]` 和 `g(r.sub, p.sub)`）。而缺口 9 给出的新 model.conf 已经去掉了 `g` 规则，改用 `regexMatch`。

```go
// 当前 Syncer 代码中的常量（旧版，不匹配 Role-in-JWT 优化）
const CasbinModelConf = "[request_definition]\nr = sub, obj, act\n\n[policy_definition]\np = sub, obj, act\n\n[role_definition]\ng = _, _\n\n[policy_effect]\ne = some(where (p.eft == allow))\n\n[matchers]\nm = g(r.sub, p.sub) && keyMatch2(r.obj, p.obj) && r.act == p.act"
```

应该更新为缺口 9 的新版。这是小问题，只需替换常量字符串即可。

### 疑点 2：APISIX 如何从 JWT 中提取 roles 数组并转为逗号分隔字符串传给 Casbin？

**问题：** 缺口 8 和 9 确认了 JWT 中 roles 是数组 `["role_admin","role_editor"]`，且 Casbin 的 `r.sub` 期望逗号分隔字符串 `"role_admin,role_editor"`。但这个转换是由 APISIX 的哪个环节完成的？

之前方案二（第二轮）的 APISIX 路由配置中：
```yaml
authz-casbin:
    username: X-User-Id
```
这里的 `username` 配置只是告诉插件从哪个 Header 提取 `r.sub`。但：
- JWT 中的 `roles` 数组是谁提取的？
- 谁把它从 JSON 数组转成逗号分隔字符串？
- 写入到哪个 Header（`X-User-Role`？）？

### 疑点 3：user_login_sso 函数未随新 JWT payload 结构更新

**问题：** 原始方案中 `user_login_sso()` 的 JWT payload 只有 `role`（单值）、`username`、`exp`。现在缺口 8 定义的新 payload 需要 `jti`、`user_id`、`username`、`tenant_id`、`dept_id`、`roles`（数组）、`exp`。

而且签名方式从之前的 `pgjwt.sign()`（HS256）改为 `generate_rs256_jwt()`（PL/Python3u）。原始 `user_login_sso()` 代码需要重写。

### 疑点 4：casbin_rule 视图需要随 Role-in-JWT 简化

**问题：** 原始方案的 casbin_rule 视图是 UNION ALL 两段（g 规则 + p 规则）。采用 Role-in-JWT 后，g 规则不再需要，视图只需保留 p 规则。缺口 10 的物化视图中已经体现了这个简化，但普通视图（小规模起点）没有对应的更新版本。

### 疑点 5：角色变更的即时生效机制

**问题：** 采用 Role-in-JWT 后，角色信息在 JWT 中。如果管理员修改了某用户的角色：
- 旧 JWT（含旧角色）还在有效期内（最多 15 分钟），用户在这 15 分钟内仍能使用旧权限
- 是否需要配合 token_blacklist 实现"即时踢下线"来强制角色变更立即生效？
- 第一轮的"踢下线"方案（db-pre-request + sys_token_blacklist）与 Role-in-JWT 的结合方式是什么？

---

## 最终评估

| 维度 | 评估 |
|:---|:---|
| 架构完整性 | 所有组件可执行、所有接口已定义、所有数据模型已建表 |
| 代码可执行性 | Policy Syncer 有完整 Go 代码、PL/pgSQL 函数有完整 SQL、APISIX 配置有完整 YAML/JSON |
| 一致性 | 少数整合细节需要对齐（疑点 1-4），但不影响架构正确性 |
| 可开发状态 | **已达到可开始开发的状态**，剩余疑点可在整合最终方案文档时一并解决 |

---

## 建议

### 方案 A：不再追问，直接进入整合阶段

当前 10 个缺口全部闭合，剩余的 5 个疑点都是**整合对齐性质**的问题，不涉及新的架构决策。你可以：
1. 把三轮专家回复整合成一份最终方案文档
2. 在整合过程中自己处理疑点 1-5（都是简单的代码对齐全）
3. 直接开始第一阶段开发

### 方案 B：再追问一轮（针对 5 个疑点做最终对齐）

如果希望专家帮你完成最后的整合对齐，可以发一轮"收尾性追问"：

> "所有 10 个缺口已经解决，非常感谢。最后还有 5 个整合对齐问题：
> 1. 请给出更新后的 Policy Syncer 中 CasbinModelConf 常量（使用 Role-in-JWT 优化后的 model.conf）
> 2. APISIX 中如何将 JWT 的 roles 数组提取为逗号分隔字符串传给 Casbin 的 r.sub？请给出完整路由配置 YAML
> 3. 请给出使用 PL/Python3u + RS256 签名、payload 含 roles 数组的 user_login_sso() 完整代码
> 4. 请给出 Role-in-JWT 优化后的 casbin_rule 视图（仅含 p 规则，无 g 规则）的完整 SQL
> 5. Role-in-JWT 下，角色变更如何强制即时生效？是否需要配合 token_blacklist？"

**我的建议：方案 B，再追问一轮收尾。** 这 5 个问题本质是让专家帮你做"最终代码对齐"，拿到后你手里的所有代码片段就是一整套可直接拼接的系统，不需要自己在拼接时猜测和调整。
