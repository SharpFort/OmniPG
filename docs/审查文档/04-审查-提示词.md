# 📋 04-网关与同步器 审查提示词

> **使用说明：** 将以下内容完整复制到新的对话中，AI Agent 会按照此提示词对 04 文档进行深度审查。

---

## 任务：深度审查 04-网关与同步器-APISIX配置与PolicySyncer.md

### 项目背景
- **项目根目录：** `D:\WeChat Files\xiangmu`
- **项目目标：** 构建基于 PG + PostgREST + APISIX + Casbin + Casdoor + Policy Syncer(Go) 的零后端代码数据库驱动 Admin 管理系统
- **审查对象：** `04-网关与同步器-APISIX配置与PolicySyncer.md`（544行）

### 前置条件（已通过审查）
- ✅ 01-环境搭建 v5.1 已审查通过
- ✅ 02-数据库建模 v2.0 已审查通过（JWT委托Casdoor、Soft Delete全覆盖、RLS 11表、审计表+pg_cron）
- ✅ 03-API与认证层 v1.0 已审查通过（22项问题已100%修复，产出审查报告+修订文档）
- ✅ PostgREST v14 已部署，连接 Pgbouncer
- ✅ APISIX 3.17.0 已内置 authz-casdoor + authz-casbin 插件

---

## 一、原始文档（请完整阅读以下三份）

### 1.1 04-网关与同步器-APISIX配置与PolicySyncer.md（审查对象，544行）

> **请阅读完整原文：** `D:\WeChat Files\xiangmu\04-网关与同步器-APISIX配置与PolicySyncer.md`

### 1.2 00-项目总纲-背景理念技术选型.md（架构参考，636行）

> **请阅读完整原文：** `D:\WeChat Files\xiangmu\00-项目总纲-背景理念技术选型.md`
> 重点关注 §2.3 Role-in-JWT 优化、§6.2 JWT 链路（v4 更新）、§6.3 RLS 行级安全

### 1.3 04-审查-网关与同步器问题清单.md（184行）

> **请阅读完整原文：** `D:\WeChat Files\xiangmu\审查文档\04-审查-网关与同步器问题清单.md`

---

## 二、其他文档的关键内容摘要（无需阅读原文）

### 2.1 02-数据库建模 v2 已锁定决策（关键约束）

| 决策项 | 锁定结论 | 影响04审查 |
|:---|:---|:---|
| JWT 签发架构 | **Casdoor RS256 签发**，PostgREST 仅验签 | 04 的 `jwt-auth` 插件必须使用 Casdoor JWKS 公钥 |
| JWKS 端点 | `/.well-known/jwks`（无 `.json` 后缀） | APISIX mocking 路由的 URI 必须是 `/well-known/jwks` |
| RT 存储 | 仍存 PG `sys_user_session`，不回退 Casdoor | refresh_token_rtr 流程保持 |
| pg_notify payload | JSON 格式 `{op, role_id, api_id, tenant_id, ts}` | Policy Syncer 必须解析 JSON 而非纯字符串 |
| 多租户模型 | 所有表有 tenant_id + RLS | Casbin 策略需配合 RLS |
| Soft Delete | 所有业务表含 deleted_at/is_active | casbin_rule 视图过滤软删除记录 |

### 2.2 03-API与认证层 v1 审查结论（影响04的一致性检查）

| 配置项 | 03 已修正值 | 04 需对齐 |
|:---|:---|:---|
| `db-schemas` | `api_v1`（移除 public） | Syncer 不应读取 public schema |
| `db-pre-request` | `api_v1.check_token_blacklist` | 函数名一致 |
| `jwt-secret` | `${JWKS_JSON}` 环境变量注入 | APISIX `jwt-auth` 也需使用 JWKS |
| `get_user_menu` | 扁平数组（含 parent_id） | 前端自行构建树 |
| Casdoor JWKS 端点 | `/.well-known/jwks` | APISIX 路由映射需一致 |

### 2.3 01-环境搭建 v5.1 端口规划（04 B1 问题的判断依据）

| 端口 | 用途 |
|:---|:---|
| 9080 | APISIX API 网关 HTTP（数据面，处理客户端请求） |
| 9180 | APISIX Admin API（控制面，管理路由/插件配置） |
| 3000 | PostgREST API |
| 5432 | PostgreSQL 开发端口（生产用 HAProxy 5433） |

---

## 三、04 文档结构概览（544行）

| 章节 | 行号 | 内容 | 核心审查点 |
|:---|:---|:---|:---|
| §1 model.conf 写入 etcd | L10-28 | Casbin 模型配置 PUT | 端口是否正确（9180 vs 9080） |
| §2.1 JWKS 端点路由 | L36-65 | mocking 插件返回 JWKS | URI 是 `/jwks.json` 还是 `/jwks`？ |
| §2.2 JWT 签名验证 | L68-77 | jwt-auth 插件配置 | 是否使用 JWKS 格式公钥 |
| §2.3 业务路由 | L80-122 | serverless-pre-function + jwt-auth + authz-casbin + CORS + limit-req | Lua 解码 JWT 逻辑是否安全？插件执行顺序？ |
| §2.4 代理重写 | L126-137 | proxy-rewrite 去除 /api/v1 前缀 | regex_uri 是否正确 |
| §3.1 Go Syncer 源码 | L147-412 | 412行 Go 代码 | 端口、Advisory Lock、错误处理、优雅关闭、CSV/MD5对账 |
| §3.2 编译 | L416-421 | go mod init + go build | 模块名是否与01一致 |
| §3.3 部署 | L425-431 | 直接运行或 systemd | 是否容器化 |
| §4 全链路验证 | L438-525 | 8个子验证 | 验证步骤是否完整 |
| §5 验收清单 | L529-544 | 10项验收 | 是否覆盖关键路径 |

---

## 四、已知问题清单（来自 04-审查-网关与同步器问题清单.md）

### 🔴 阻塞级（3个）

| # | 问题 | 影响 |
|:---|:---|:---|
| **B1** | **APISIX Admin API 端口全部写错** — 所有 curl 命令和 Go Syncer 使用 9080，但 Admin API 在 9180 | 所有管理操作 404 |
| **B2** | **CSV 格式与 DB MD5 计算不一致** — DB 用 `,` 分隔，Go 用 `, `（逗号+空格）；10分钟对账永远不匹配 | 对账机制失效 |
| **B3** | **缺少 Advisory Lock 选主** — 00 总纲要求多实例+选主，代码完全未实现 | 多实例竞态条件 |

### 🟡 重要级（6个）

| # | 问题 |
|:---|:---|
| **M1** | serverless-pre-function 手动解析 JWT 的风险（签名未验证即注入 header） |
| **M2** | keyMatch2 的 PostgREST URL 匹配语义（Query String 如何处理？） |
| **M3** | Go Syncer 多处错误被忽略（`body, _ := io.ReadAll`） |
| **M4** | 未实现优雅关闭（无 signal handling） |
| **M5** | PostgresDSN 端口 5433 与 01/03 不一致（开发环境不通） |
| **M6** | 使用 crypto/md5 对账（应改为 SHA256） |

### 🟢 增强级（5个）

| # | 问题 |
|:---|:---|
| **E1** | Go 编译指令 `go mod init policy-syncer` 与 01 的 `go mod init syncer` 不一致 |
| **E2** | JWKS 公钥占位符硬编码在多处，密钥轮换困难 |
| **E3** | CORS origin 硬编码 `https://admin.yourdomain.com`，开发环境无法使用 |
| **E4** | 未使用结构化日志（log/slog） |
| **E5** | Syncer 未容器化 |

---

## 五、审查要求

### 5.1 线上调研（必须执行）

对文档中涉及的外部组件获取最新信息：

| 组件 | 调研内容 | 禁止行为 |
|:---|:---|:---|
| APISIX 3.17.0 | `authz-casbin` / `jwt-auth` / `serverless-pre-function` 插件最新参数 | 禁止用训练数据描述 |
| Casbin Lua v2.x | `keyMatch2` / `regexMatch` 函数语义（路径匹配规则） | 必须查官方文档 |
| Casbin Go SDK v2 | `casbin/casbind` 最新版本、策略加载 API | 禁止猜测版本 |
| Go `lib/pq` | LISTEN/NOTIFY 的 `pq.NewListener` 签名 | 确认代码兼容性 |
| etcd Go client v3 | `etcd/client/v3` 最新版本与 APISIX etcd 交互 | 确认代码无需此依赖 |
| Casdoor JWKS | `/.well-known/jwks` 输出格式（已确认无 `.json` 后缀） | 确认 04 文档中 URI 是否正确 |

### 5.2 审查维度

| 维度 | 检查重点 |
|:---|:---|
| **准确性** | 端口（9080 vs 9180）、Casbin model.conf 语法、Go 函数签名、Lua JWT 解码逻辑 |
| **完整性** | Advisory Lock 缺失、优雅关闭缺失、Signal handling、错误处理 |
| **一致性** | 与 02 v2 的 pg_notify JSON payload、与 03 v1 的 JWKS 端点路径、与 01 的端口规划 |
| **安全性** | JWT 解码不验证签名即注入 header（M1）、JWKS 公钥硬编码、CORS 硬编码 |
| **性能** | 1秒防抖、10分钟对账、MD5 vs SHA256、多实例竞态 |
| **可执行性** | Go 代码能否直接编译（`go build` 无错误）、APISIX Admin API 端口 |

### 5.3 子代理分工（启动 3 个并行子代理）

| 子代理 | 任务 | 输出 |
|:---|:---|:---|
| **A：线上调研** | 获取 APISIX 插件最新参数、Casbin Lua/Go SDK 最新版本、Casdoor JWKS 实际端点 | 版本事实核对表 |
| **B：技术分析** | 分析 Go Syncer 源码的正确性/安全性/完整性；分析 APISIX 路由配置的插件执行顺序和潜在风险 | 技术审查报告 |
| **C：文档对比** | 逐行对比 04 文档与 04-审查-网关与同步器问题清单.md，验证已知问题并发现新问题 | 文档对比报告 |

---

## 六、输出物要求

| # | 产出物 | 格式 | 说明 |
|:---|:---|:---|:---|
| 1 | 审查报告 | Markdown | 每个问题的准确性判定 + 修复建议 |
| 2 | 技术审查报告 | Markdown | Go 源码问题、APISIX 插件风险、性能瓶颈 |
| 3 | 文档对比报告 | Markdown | 已知问题验证状态 + 新发现问题（含严重程度） |
| 4 | 版本事实核对表 | Markdown | 组件名/文档声称版本/实际最新版本/来源URL |
| 5 | 修订后的完整文档 | Markdown | 覆盖 04 文档（修复所有问题） |
| 6 | 优先级修复清单 | Checklist | P0/P1/P2/P3 四级排序 |

---

## 七、执行流程

```
Step 1: 加载 skill（hermes-agent、systematic-debugging）
Step 2: 阅读三份原始文档（04目标文档 + 00总纲 + 04审查问题清单）
Step 3: 输出审查方案（调研计划、子代理分工），等待确认
Step 4: 确认后启动 3 个子代理并行执行
Step 5: 汇总结果 → 审查报告 + 修订文档 + 优先级清单
Step 6: 更新 MEMORY.md（记录已通过审查的里程碑）
```

---

## 八、已知经验（来自 01/02/03 审查）

| 经验 | 说明 |
|:---|:---|
| ✅ Casdoor JWKS 端点无 `.json` 后缀 | 04 文档 §2.1 的 `uri: /well-known/jwks.json` 应改为 `/well-known/jwks` |
| ✅ JWT 签发已委托 Casdoor | 04 不能有 plpython3u 或本地 JWT 签名代码 |
| ✅ APISIX Admin API 端口是 9180 | 04 文档所有 curl 命令和 Go 代码的 Admin API URL 需检查端口 |
| ✅ pg_notify payload 是 JSON | Policy Syncer 必须解析 JSON payload，不能只读 channel name |
| ✅ 开发/生产环境通过 .env 管理 | 配置文件需支持环境变量替换 |
| ⚠️ GitHub API 可能受限 | 如 curl 失败，使用 web_search 作为 fallback |

---

## 九、审查重点（避免重蹈 02/03 审查中发现的问题）

| 02/03 典型错误 | 04 对应检查点 |
|:---|:---|
| JWT 签发架构冲突 | jwt-auth 插件是否使用 JWKS 公钥？公钥占位符是否替换为环境变量？ |
| Admin API 端口错误 | 所有 curl 命令和 Go 常量是否使用 9180 而非 9080？ |
| RLS 仅覆盖部分表 | casbin_rule 视图过滤软删除记录 → Syncer 读取的视图结果一致 |
| 缺 updated_at 自动更新 | 04 不涉及建表，但 Go 代码的时间字段处理需检查 |
| pg_notify payload 过简 | Go Syncer 是否解析 JSON payload（op/role_id/api_id/tenant_id/ts）？ |
| CSV 格式与 MD5 不匹配 | DB 侧 `concat_ws(',', ...)` 与 Go 侧 `strings.Join(elements, ", ")` 分隔符不一致 |
| 缺少 Advisory Lock | 00 总纲要求多实例选主 → Go 代码需实现 `pg_try_advisory_lock` |
| 未容器化 | Syncer 是否加入 Docker Compose？ |

---

## 十、审查通过标准

- ✅ 所有 🔴 P0 阻塞级问题已修复（端口、CSV/MD5、Advisory Lock）
- ✅ 所有 🟡 P1 重要级问题已修复或明确标注为"设计保留"
- ✅ Go 代码可直接 `go build` 编译无错误
- ✅ APISIX 路由 JSON 可通过 Admin API 直接 PUT 创建
- ✅ 与 02 v2 / 03 v1 / 01 v5.1 的跨文档一致性确认
- ✅ 验收清单覆盖所有关键路径

---

> **本提示词由主代理根据 01/02/03 审查经验自动生成，确保审查标准一致、无遗漏。**
