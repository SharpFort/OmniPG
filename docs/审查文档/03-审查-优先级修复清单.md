# 03-API与认证层 优先级修复清单

> **审查日期：** 2026-07-08
> **审查对象：** `03-API与认证层-PostgREST配置与接口验收.md`
> **总计：** 22 项问题（🔴P0 ×5 | 🟡P1 ×10 | 🟢P2 ×4 | ⚪P3 ×3）

---

## 🔴 P0 阻塞级（必须修复才能启动 PostgREST）

| # | 问题 | 修复方式 | 状态 |
|:---|:---|:---|:---|
| **P0-1** | `db-schemas = "api_v1, public"` 暴露 public schema | 改为 `db-schemas = "api_v1"`，RPC 函数迁移到 api_v1 schema | ✅ 已修复 |
| **P0-2** | `api_v1` 视图 `SELECT *` 暴露 `password_hash` | 改为显式列白名单，排除 password_hash | ✅ 已修复 |
| **P0-3** | `check_token_blacklist` 函数未定义 | 在 api_v1 schema 中创建函数（含 search_path + 401 返回） | ✅ 已修复 |
| **P0-4** | `jwt-secret` 中 RSA 模数 `n` 为占位符 | 改为环境变量 `${JWKS_JSON}` 注入 | ✅ 已修复 |
| **P0-5** | `get_user_menu` 预期响应（嵌套树）与实际实现（扁平数组）不匹配 | 更新文档预期响应为扁平数组格式 | ✅ 已修复 |

---

## 🟡 P1 重要级（功能不完整或存在缺陷）

| # | 问题 | 修复方式 | 状态 |
|:---|:---|:---|:---|
| **P1-1** | `jwt-role-claim-key = ".roles[0]"` 只取第一个角色 | 确认角色切换策略（.roles[0] → authenticated），补充说明 | ✅ 已修复 |
| **P1-2** | `db-pre-request` 缺少 schema 前缀 | 改为 `api_v1.check_token_blacklist` | ✅ 已修复 |
| **P1-3** | CORS 策略完全缺失 | 添加 `cors-origins` 和 `cors-headers` 配置 | ✅ 已修复 |
| **P1-4** | `submit_role_request` RPC 不存在 | 在 api_v1 schema 中创建函数 | ✅ 已修复 |
| **P1-5** | Swagger UI `API_URL` 使用 Docker 内部主机名 | 改为 `http://localhost:3000/`（已在 01 中配置） | ✅ 已修复 |
| **P1-6** | `check_token_blacklist()` 缺少 `search_path` 设置 | 函数内添加 `SET search_path = public, pg_temp` | ✅ 已修复 |
| **P1-7** | `db-uri` 端口硬编码 5432 | 使用环境变量 `${DB_HOST}` / `${DB_PORT}` | ✅ 已修复 |
| **P1-8** | `db-extra-search-path` 注释含义不准确 | 移除多余声明，修正注释 | ✅ 已修复 |
| **P1-9** | 缺少 `authenticator` 角色对业务角色的 GRANT | 补充 GRANT 语句（web_anon / authenticated → authenticator） | ✅ 已修复 |
| **P1-10** | 验收清单缺少 RLS 策略验证 | 补充 A11-A15 验收项 | ✅ 已修复 |

---

## 🟢 P2 增强级（不影响功能但值得完善）

| # | 问题 | 修复方式 | 状态 |
|:---|:---|:---|:---|
| **P2-1** | curl 命令使用 Linux/macOS 语法 | 增加 Windows PowerShell 版本 | ✅ 已修复 |
| **P2-2** | Swagger UI 配置与 01 重复 | 统一到 01，03 引用 | ✅ 已修复 |
| **P2-3** | `refresh_token_rtr` curl 命令冗余 Cookie 头 | 删除 Cookie 头，仅保留 `-d` 参数 | ✅ 已修复 |
| **P2-4** | 验收清单不完整 | 补充 JWT 验签、RLS 隔离、Token 过期验证 | ✅ 已修复 |

---

## ⚪ P3 细节（建议性）

| # | 问题 | 修复方式 | 状态 |
|:---|:---|:---|:---|
| **P3-1** | `pg_graphql` 端点未声明 | 明确声明是否启用 | ✅ 已修复 |
| **P3-2** | APISIX 配置范围外 | 添加指向 04 文档的引用 | ✅ 已修复 |
| **P3-3** | `db-uri` 密码明文占位 | 说明密码管理方案（.env + Docker secrets） | ✅ 已修复 |

---

## 跨文档一致性修复

| 检查项 | 01 文档 | 02 文档 | 03 文档 | 一致性 |
|:---|:---|:---|:---|:---|
| PostgreSQL 端口 | 5432 | N/A | 5432（环境变量化） | ✅ 一致 |
| JWT role claim | 未配置 | `roles[0]` | `roles[0]` | ⚠️ 01 缺失（04 范围） |
| get_user_menu 格式 | N/A | 扁平数组 | 扁平数组 | ✅ 一致 |
| submit_role_request | N/A | 已创建 | 已创建 | ✅ 一致 |
| Swagger UI | 有（v5） | N/A | 引用 01 | ✅ 一致 |
| RLS 策略 | N/A | 11表全覆盖 | 依赖 02 | ✅ 一致 |

---

## 修复统计

| 级别 | 数量 | 已修复 | 修复率 |
|:---|:---:|:---:|:---:|
| 🔴 P0 | 5 | 5 | 100% |
| 🟡 P1 | 10 | 10 | 100% |
| 🟢 P2 | 4 | 4 | 100% |
| ⚪ P3 | 3 | 3 | 100% |
| **合计** | **22** | **22** | **100%** |

---

## 产出物清单

| # | 文件 | 说明 |
|:---|:---|:---|
| 1 | `审查文档/03-审查-深度审查报告.md` | 完整审查报告（含版本核对、问题清单、一致性矩阵） |
| 2 | `03-API与认证层-PostgREST配置与接口验收.md` | 修订后的完整文档（已修复所有 P0/P1/P2/P3） |
| 3 | `审查文档/03-审查-优先级修复清单.md` | 本清单 |

---

## 后续建议

1. **02 文档同步**：03 文档中引用的 `check_token_blacklist`、`submit_role_request` 等函数需在 02 Migration 中同步创建
2. **04 文档衔接**：APISIX 路由配置、Policy Syncer 实现见 04 文档，本次审查未覆盖
3. **运行时验证**：部署后需验证 `db-pre-request` 函数的 401 返回行为（使用 `RAISE sqlstate 'PT401'`）
4. **JWKS 环境变量**：生产环境需通过 Docker secrets 或 CI/CD 管道注入 `JWKS_JSON`
