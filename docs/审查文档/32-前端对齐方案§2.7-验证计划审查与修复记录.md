# 32-前端对齐方案 §2.7 验证计划审查与修复记录（全方案闭环）

> **审查对象**：`docs/1.前端对齐后端方案-修订版.md` §2.7 验证计划（+ §三 执行顺序 / §四 风险连带）
> **审查日期**：2026-08-07
> **状态**：✅ 用户已拍板（2026-08-07，3 项决策）；文档 v1.7/v3.10 已落盘
> **意义**：§1.1 → §2.7 **全方案审查闭环**（11 个章节，8 轮审查，9 个审查文档）

---

## 一、审查结论总览

| 编号 | 问题 | 级别 | 处置 |
|:---|:---|:---|:---|
| V-1 | "注册流程（若可行）"与 §2.1 全托管脱节（注册页已删，验证 = signUp 模式托管注册） | P1 | ✅ 重写为 A-4（注册按钮 → 托管注册 → 回调建档 → JIT 首登验证） |
| V-2 | 忘记密码验证缺失（§2.1 修正后核心认证流） | P1 | ✅ 新增 A-5（托管 Forgot password 全流程） |
| V-3 | 登出验证缺失（signOut → userStore 清空 → 未登录跳转） | P1 | ✅ 新增 A-6 |
| V-4 | tenant_admin 权限矩阵无可勾选断言（§2.5 定稿的 ✅/❌/📖 矩阵） | P1 | ✅ 新增 B-2（按钮矩阵断言）+ B-3（login-log 降级）+ B-4（cron 静默空） |
| V-5 | 新用户首次登录 JIT 验证缺失 | P2 | ✅ A-4 内置（v_user_roles 出现新用户 + 角色绑定） |
| V-6 | v-perm 显隐 + usePermission 缓存验证缺失 | P2 | ✅ B-5（切换账号缓存清空） |
| V-7 | token 静默刷新验证缺失 | P3 | ✅ A-7 |
| V-8 | 浏览器工具不具体（本机 Chrome 实测未装） | P3 | ✅ 拍板 3：`pnpm dlx playwright install chromium` |
| V-9 | 42501 全局提示验证缺失 | P3 | ✅ D-1（B-3 覆盖） |
| V-10 | §三 执行顺序"认证页（注册/忘记密码/登出）"过时 | P3 | ✅ 修订：认证（登录页两按钮/回调页/登出） |
| V-11 | §四 风险"v_user_roles 受 RLS 限制仅超管完整"过时 | P3 | ✅ 删除（v1.5 确认全量可见）+ 补 4 项新风险 |
| V-12 | Console 前置配置未纳入验证前置条件 | P3 | ✅ P-1~P-7（apply-src/SPA 应用/登录方式/connector/密码策略/organization_id/测试账号） |

---

## 二、用户拍板记录（2026-08-07）

| # | 拍板 | 落地 |
|:--|:--|:--|
| 1 | tenant_admin 测试账号：**用户在 Logto Console 创建**（含组织成员） | P-7 |
| 2 | 注册/忘记密码验证依赖 connector：**配置 Email/SMTP（Resend 均可）**，完整验证认证流 | P-4 + A-4/A-5 不跳过 |
| 3 | 浏览器工具：采纳 `pnpm dlx playwright install chromium` | V-8 |

---

## 三、v1.7 验证计划结构（定稿）

```
前置条件 P-1~P-7（宿主 apply-src/PostgREST/角色映射 + Console SPA/登录方式/connector/密码策略/organization_id/测试账号）
1. 静态检查（lint + build，含 signIn 字符串枚举、snake_case 类型）
2. 启动（pnpm dev 5173）
3. 认证流 A-1~A-7（未登录跳转/两按钮/登录/注册+JIT/忘记密码/登出/token 静默刷新）
4. 权限矩阵 B-1~B-5（超管全真/tenant_admin ✅❌📖 断言/42501 降级/cron 静默空/缓存清空）
5. 页面数据 C-1~C-5（13 页面/分页 clamp/镜像对照/菜单 CRUD/权限分配父子联动/树渲染）
6. 边界 D-1~D-3（42501/回调 error/空树 fallback）
7. 提交推送
```

---

## 四、全方案审查闭环总结（§1.1 → §2.7）

| 轮次 | 章节 | 迁移/文档 | 关键修复 |
|:--|:--|:--|:--|
| 1 | §1.1 视图清单 | 034 + 027 重写 + postgrest.conf 同步 | user_role 语义错位、缺 user_tenants、6 视图补授权、v_role_list.users_count、pg_cron 失效路径 |
| 2 | §1.2 RPC 清单 | 035（+多轮补丁） | 删 health_check/export_csv/rpc_sync_user_roles/get_user_permissions/cleanup_expired_tokens 整链；import_csv 安全重写；分页 clamp 100；ensure_user JIT；require_* helper；get_dept_tree text 参数；cron GRANT；get_user_menu +menu_type/component；is_super_admin P0 重建；create_menu +is_visible；tenant_admin 补绑 |
| 3 | §2.2 API 层 | 文档 v1.3 | 模块划分定稿、request.ts 设计、sync_user_roles 残留清除 |
| 4 | §2.3 类型 | 文档 v1.4 | snake_case 透传、26 类型清单、菜单类型分层 |
| 5 | §2.4 菜单路由 | 文档 v1.4 | component 链路、两遍构建、hideInMenu、父子联动、backend 模式下一轮 |
| 6 | §2.5 页面 | 文档 v1.5 | 页面参数矩阵、按钮矩阵、login-log 筛选器、全量可见性标注 |
| 7 | §2.6 权限 | 文档 v1.5 | 短路收窄、内存缓存、v-auth/v-perm 边界 |
| 8 | §2.1 认证 | 文档 v1.6 | 全托管、忘记密码托管、signIn 签名、密码策略清单 |
| 9 | §2.7 验证计划 | 文档 v1.7（本轮） | 分层验证结构、前置条件、风险修订 |

**审查文档**：27 ~ 32 号（6 篇）+ 24/26 号修订关联。
**验证链**：PGlite 真实执行 49/49 + sqlglot 0 错误（035 全量 + 行为断言），本迭代捕获并修复 6 个真实运行期缺陷（import_csv DEFAULT 列、jsonb_object_length、pg_get_function_identity_arguments、fixture 顺序、regprocedure 字符串、cron 死链）。

---

## 五、遗留与后续（跨轮次汇总）

| 项 | 说明 | 状态 |
|:---|:---|:---|
| 视图 invoker 化 | 31 视图 security_invoker=true + 012/017 策略核对（个人信息跨租户可见的根治） | 🔜 单独一轮（P1） |
| backend 菜单模式 | 登录后拉取 get_user_menu+v_role_api_detail 入 Pinia 缓存、动态 addRoute | 🔜 下一轮 |
| usePermission Pinia 缓存 | 与 backend 模式同批 | 🔜 下一轮 |
| organization_id claim | Console Custom JWT 核对（后端 current_tenant_id 依赖） | ⏳ 用户 Console 操作 |
| 密码策略配置 | Console > Security > Password policy 按 NIST 表 | ⏳ 用户操作 |
| Email/SMTP connector | 忘记密码/注册验证前置 | ⏳ 用户操作 |
| tenant_admin 测试账号 | Console 创建（含组织成员） | ⏳ 用户操作 |
| 宿主 apply-src | 035（含全部补丁）重放生效 | ⏳ |
| OmniAdmin 实施 | 按 §2.1-§2.7 定稿实施（前端仓库） | ⏳ |
