# 2. OmniAdmin 前端实施计划（基于前端对齐后端方案 v1.7）

> 制定日期：2026-08-07
> 依据：`docs/1.前端对齐后端方案-修订版.md`（v1.7，§1.1-§2.7 全部定稿）+ 审查文档 27~32 号
> 执行方式：**分 9 个阶段逐项推进**，每阶段含任务清单（可勾选）、验收标准、结果项；建议在独立分支 `feature/frontend-alignment` 上开发，每阶段一个 commit（message 规范：`feat|fix|docs: [Phase N] 描述`）
> 后端基线：OmniPG `feature/logto-authn`（035 迁移含全部补丁）；宿主需完成 `apply-src.sh` 全量重放

---

## 〇、前置条件（P-1 ~ P-7，未满足的项对应阶段会受阻，先确认）

| # | 前置条件 | 责任方 | 状态 |
|:--|:--|:--|:--|
| P-1 | 宿主 OmniPG 完成 `apply-src.sh` 全量重放（035 生效）+ PostgREST 重启 + `jwt-role-claim-key` 角色映射核对 | 后端 | ☐ |
| P-2 | Logto Console：SPA 应用创建 + redirect URI（`http://<前端开发机IP>:5173/auth/callback`）+ post sign-out redirect URI（`http://<前端开发机IP>:5173/`）+ **CORS 允许来源加入开发机地址** | 前端+后端协调 | ☐ |
| P-3 | Logto Console：Sign-up and sign-in 启用 Password 登录方式 + 注册标识 + Forgot password 验证方式 | 后端 | ☐ |
| P-4 | Email/SMTP connector 配置（Resend/SMTP 均可——注册/忘记密码完整验证） | 后端 | ☐ |
| P-5 | Logto Console：Password policy 按 NIST 表配置（§2.1） | 后端 | ☐ |
| P-6 | Custom JWT 核对 `organization_id` claim（后端 current_tenant_id 依赖） | 后端 | ☐ |
| P-7 | 测试账号：testC（role_super_admin）+ tenant_admin（含组织成员）就绪 | 后端 | ☐ |

---

## 阶段总览

| 阶段 | 名称 | 对应章节 | 验收核心 |
|:--|:--|:--|:--|
| Phase 0 | 环境准备与基线 | — | dev 跑通 + lint/build 0 错 |
| Phase 1 | 类型定义层 | §2.3 | vue-tsc 通过，26 类型与后端字段一致 |
| Phase 2 | API 层重写 | §2.2 | 7 个模块函数签名与清单一致 |
| Phase 3 | 认证流程（全托管） | §2.1 | A-1~A-7 认证流通过 |
| Phase 4 | 路由与菜单 | §2.4 | 本地路由全量 + 适配逻辑预留 |
| Phase 5 | 系统管理页面 | §2.5 | 13 页面数据加载 + 按钮矩阵 |
| Phase 6 | 权限指令 | §2.6 | B-1~B-5 权限断言通过 |
| Phase 7 | 联调验证 | §2.7 | A/B/C/D 全量清单通过 |
| Phase 8 | 收尾发布 | — | 提交推送 + 文档更新 |
| Phase 9 | 遗留优化（下一轮） | 见 §九 | invoker 化（P1 安全）/ backend 菜单 / Pinia 缓存 / 实测落地 |

---

## Phase 0：环境准备与基线

**目标**：另一台电脑上把 OmniAdmin 跑起来，确认与后端/Logto 的连通性。

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 0.1 | 克隆前端仓库（如 SharpFort/OmniAdmin），`pnpm install` | 依赖安装 0 错误 | 本地仓库 |
| 0.2 | 确认后端可达：`curl http://<宿主IP>:3100/` 返回 PostgREST 信息；Logto `curl http://<宿主IP>:3001/oidc/.well-known/openid-configuration` 200 | 两个端点可达 | 连通性记录 |
| 0.3 | 配置 `.env`：`VITE_API_BASE`（宿主 PostgREST）、`VITE_LOGTO_ENDPOINT`、`VITE_LOGTO_APP_ID`、`VITE_LOGTO_REDIRECT_URI`（含开发机 IP） | env 模板提交仓库（`.env.example`），敏感值不入库 | .env + .env.example |
| 0.4 | `pnpm dev` 启动（5173）+ 现有登录页能跳转 Logto 托管页（旧行为基线） | dev server 启动，点击登录跳 Logto | 基线记录 |
| 0.5 | `pnpm run lint` + `pnpm run build` | 0 error（warning 可接受） | 基线报告 |

**结果项**：环境就绪记录（后端地址/Logto 应用/测试账号）

---

## Phase 1：类型定义层（§2.3）

**目标**：`src/types/api/api.d.ts` 重写为全量类型清单（**snake_case 透传，零转换层**）。

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 1.1 | 统一泛型：`PageResult<T>` / `ApiOk` / `MenuType`（四值联合） | 与 §2.3 定义一致 | api.d.ts |
| 1.2 | 认证/用户：`UserInfo`（roles: string[]）、`UserListItem`（organizations: string[]）、`UserRoleRow`（role_code: null 可）、`UserProfile` | 字段与 get_current_user/v_user_list 实测返回一致 | api.d.ts |
| 1.3 | 菜单三型：`MenuRouteItem`（含 menu_type/perms/is_visible/**component**）、`MenuAdminNode`（iam_menu 全列）、`RouteMetaNode`（meta.roles/hideInMenu） | 与 get_user_menu/iam_menu 视图一致 | api.d.ts |
| 1.4 | 组织/部门/岗位/字典/租户：`DeptNode`（level/path）、`PositionNode`（**depth/path_name**）、`DictType`/`DictData`/`DictListItem`、`Tenant`（member_count）、`TenantMember` | 与对应视图/RPC 返回一致（§2.3 表格） | api.d.ts |
| 1.5 | 日志/监控：`LoginLog`、`AuditLog`（以 search_audit_log 为准）、`AuditLogTimeline`、`SystemStats`、`SystemStatsRealtime`（两列 null）、`CronJob`/`CronJobRun`（数组） | 字段逐一对照 §2.3 | api.d.ts |
| 1.6 | 角色/权限：`RolePermissionDetail`、`RoleApiPerm`（api_code） | 与 get_role_permissions/v_role_api_detail 一致 | api.d.ts |
| 1.7 | 全量 nullable 标注（`*_id`/`role_code`/`region`/`component`） | vue-tsc 通过 + 与后端 null 行为一致 | api.d.ts |

**验收**：`pnpm run build`（vue-tsc）0 错误；与后端实测返回逐字段比对（Postman/浏览器 Network 对照）无遗漏。
**结果项**：`src/types/api/api.d.ts` 全量重写

---

## Phase 2：API 层重写（§2.2）

**目标**：统一请求封装 + 7 个 API 模块，函数签名与 §1.2 权威清单一一对应。

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 2.1 | `src/api/request.ts`：`postRpc<T>` / `getView<T>` / `getViewPage<T>`（Content-Range total + count=exact）/ 错误拦截（**42501 → 全局提示**，P0001 → 业务提示）/ Bearer 注入（@logto/vue fetchResource 或 getAccessToken） | 3 个导出函数 + 拦截器；用浏览器 Network 验证 Authorization 头 | request.ts |
| 2.2 | `auth.ts`：`ensureUser` / `getCurrentUser` / `getAllPublicConfigs` / `getConfig`；**删除** `fetchLogout` / `fetchGetUserPermissions` | 4 函数；旧函数无残留引用（grep 0） | auth.ts |
| 2.3 | `system-manage.ts` 重写：用户（searchUsers/v_user_list/v_user_roles/getUserProfile/updateUserProfile）、角色（v_role_list/v_role_users/getRolePermissions/setRoleApis/**setRoleMenus**）、部门（getDeptTree+CRUD）、岗位（rpcGetPositionTree+CRUD+**assignUserPositions** 数组参数）、菜单（iam_menu 视图/CRUD/getMenuTreeAdmin 只读）、配置（updateConfig/config_admin） | 函数签名与 §1.2 一致；数组参数传 JSON 数组；**删除 Casdoor 残留 10 个函数（create_user / update_user_status / assign_role_to_user / batch_assign_roles / get_user_roles / update_role_permissions / get_role_users / force_logout_user / reset_user_password / get_user_sessions），grep 0 命中** | system-manage.ts |
| 2.4 | `audit.ts`：searchAuditLog / getAuditLogTimeline / v_audit_log_detail / v_audit_log_timeline 查询 | 4 函数 + AuditLogTimeline 独立类型 | audit.ts |
| 2.5 | `monitor.ts`：v_system_stats / v_system_stats_realtime / rpcListCronJobs / rpcListCronJobRuns（**数组返回**） | 4 函数，CronJob[] 类型 | monitor.ts |
| 2.6 | `dict.ts`：v_dict_list / dict_type / dict_data 视图 + 6 个 CRUD RPC | 8 函数 | dict.ts |
| 2.7 | `tenant.ts`：rpcListTenants / rpcListTenantMembers / user_tenants 视图 / v_user_role_detail | 4 函数 | tenant.ts |
| 2.8 | `import.ts`（可选占位）：import_csv 标注"待使用再决策"，不实现 | 文件存在 + 注释说明 | import.ts |

**验收**：全部函数签名与 §1.2 清单逐项核对（自查表）；`grep -r "fetchLogout\|fetchGetUserPermissions\|create_user\|force_logout" src/api` 0 命中；登录后手动调用 3 个代表性 RPC（get_current_user/search_users/rpc_list_tenants）返回正常。
**结果项**：`src/api/` 全套（request/auth/system-manage/audit/monitor/dict/tenant/import）

---

## Phase 3：认证流程（Logto 全托管，§2.1）

**目标**：登录/注册/回调/登出全部交由 Logto；删除注册页、忘记密码页。

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 3.1 | `main.ts`：createLogto config 补 `scopes: [UserScope.Email, UserScope.Phone]`（resources 不配） | config 生效（Network 看 authorize 请求 scope 参数） | main.ts |
| 3.2 | 登录页 `/auth/login`：仅两个按钮——「统一身份认证登录」`signIn(redirectUri)`、「注册账号」`signIn(redirectUri, 'signUp')`（**字符串枚举**）；删除本地表单；redirectUri 读 `VITE_LOGTO_REDIRECT_URI`（与 0.3 的 env 一致） | 页面渲染两按钮；点击跳 Logto 托管页 | Login.vue |
| 3.3 | **删除** `/auth/register` 注册页组件（假注册 setTimeout 代码） | 路由/文件删除；访问 /auth/register 404 或跳登录 | 删除记录 |
| 3.4 | **删除** `/auth/forget-password` 空壳页 | 同上 | 删除记录 |
| 3.5 | 回调页 `/auth/callback` 四步：① 检测 URL `error`/`error_description` → 提示 + `router.replace('/auth/login')`；② 成功 → `ensureUser()`（**失败：重试 1 次，仍失败提示 + 跳登录页**）；③ `getCurrentUser()` 填 userStore；④ `router.replace(redirect)`（登录前页面） | A-2/A-3 通过；取消登录场景（D-2）提示正常 | Callback.vue |
| 3.6 | 登出：`signOut(postLogoutRedirectUri)` → 清 userStore → 跳登录页 | A-6 通过（登出回站、userStore 空、访问 / 跳登录） | logout 逻辑 |
| 3.7 | **路由守卫与登录态生命周期**（A-1/A-2 基础设施）：`router.beforeEach`——① 未登录访问受保护路由 → `/auth/login`（A-1）；② 已登录访问 `/auth/login` → 首页；③ **F5/直链恢复**：`isAuthenticated` 为 true 但 userStore 空 → `getCurrentUser()` 重新拉取，失败提示重试 | A-1 通过；F5 刷新后页面数据完整；直链深链可访问 | 路由守卫 |

**验收**：§2.7 认证流 A-1~A-7 全部通过（P-2~P-4 已满足前提下）：
- A-1 未登录访问 / → 登录页；A-2 两按钮；A-3 testC 登录 → 回调 → 首页；A-4 新账号注册 → 建档 → 首页（**首次登录 JIT 验证：v_user_roles 出现新用户**）；A-5 忘记密码托管流程；A-6 登出闭环；A-7 token 静默刷新。
**结果项**：认证流走通记录（含 JIT 验证截图/数据）

---

## Phase 4：路由与菜单（§2.4）

**目标**：本地路由全量（frontend 模式）+ 后端菜单适配逻辑实现但默认关闭。

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 4.1 | `router/modules/system.ts`：roles 更新为 `['role_super_admin','tenant_admin']` | 路由 meta 更新 | system.ts |
| 4.2 | 新增子路由：dept、position、dict、tenant、login-log、audit-log、monitor（含组件懒加载） | 7 个新路由可直接访问对应页面 | 路由模块 |
| 4.3 | `MenuProcessor.processBackendMenu`（实现但默认关闭）：扁平→树**两遍构建**（先 Map 后挂载，不假设父先于子）；component 直用后端值 + 映射表**兜底**（11 项）；menu_type=button 过滤 + link 外链处理（不 addRoute）；is_visible=false → **meta.hideInMenu**（注册不显示）；空树 fallback 本地 asyncRoutes | 单测/手动验证：乱序 order_num 组树正确；button 不入路由；hideInMenu 生效 | MenuProcessor.ts |
| 4.4 | 权限分配页父子联动准备（随 Phase 5 角色页）：穿梭框勾选子节点自动带父链 | Phase 5 验收 | — |
| 4.5 | **v-auth/authList 关系明确**：v-auth 保持现有机制（路由 meta.authList，页面级，art-design-pro 惯例）；新增页面路由按惯例配置 authList；**不与 v-perm 权限码（api_code 体系）混用**（§2.6 职责边界） | README/注释说明 + 新增路由 authList 齐全 | 路由模块 |

**验收**：frontend 模式下所有路由可访问；切换 `VITE_ACCESS_MODE=backend` 后适配逻辑输出正确菜单树（临时验证后切回 frontend）。
**结果项**：路由/菜单代码 + 适配逻辑验证记录

---

## Phase 5：系统管理页面（§2.5）

**目标**：13 个页面按参数矩阵实现（数据源/操作/权限按钮），分两个子阶段推进。

**前置 5.0 测试数据准备**：部门/岗位/字典类型+数据项各 2-3 条（011 只 seed 了 iam_menu/iam_api；department/dict 无种子——验证时通过页面 CRUD 或后端 SQL 创建）；用户资料数据（avatar 等）在 user-center 验证时设置。

### Phase 5a：基础四页 + 用户/角色（user/role/menu/api）

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 5a.1 | user 用户管理：v_user_list + searchUsers（p_query/p_status 三态/**p_dept_id 部门下拉（数据源 getDeptTree）**）+ 编辑资料（getUserProfile/updateUserProfile）+ 查看角色（v_user_roles）；删除/禁用 → Logto 侧提示 | 列表/搜索/分页正常；资料编辑白名单字段可改（tenant_id/dept_id 不可改） | UserList.vue |
| 5a.2 | role 角色管理：v_role_list + 权限分配（**iam_menu 全量视图树** + getRolePermissions 已选回显 + setRoleApis/setRoleMenus + **父子联动**）+ 成员（v_role_users）；新增/删除 → Logto 提示 | C-4 通过：分配树区分 menu/button、勾选子自动带父链、提交后重查回显一致 | RoleList.vue |
| 5a.3 | menu 菜单管理：**iam_menu 视图（GET 全列）+ 前端组树**；树形表格 + create（**含 is_visible 开关**）/update/delete_menu；getMenuTreeAdmin 仅只读概览 | C-3 通过：新建菜单含显示状态、编辑回显全字段 | MenuList.vue |
| 5a.4 | api API 管理：iam_api 视图列表（api_code/path/method/name/is_active） | 列表渲染 + 关键词过滤 | ApiList.vue |

### Phase 5b：新增六页 + 个人中心/仪表盘

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 5b.1 | dept 部门管理：getDeptTree（level/path 渲染）+ create/update/delete_department（p_parent_id uuid/p_sort_order） | C-5 部门树渲染 + CRUD | DeptList.vue |
| 5b.2 | position 岗位管理：rpcGetPositionTree（depth/path_name）+ CRUD + **assignUserPositions（数组参数）** | 岗位树 + 分配弹窗（多选岗位 + 主岗位） | PositionList.vue |
| 5b.3 | dict 字典管理：v_dict_list 两级（类型 + 数据项）+ 6 个 CRUD RPC | 类型树/表格 + 数据项子表 CRUD | DictList.vue |
| 5b.4 | tenant 租户管理：rpcListTenants + rpcListTenantMembers（成员查看） | 列表（member_count）+ 成员弹窗 | TenantList.vue |
| 5b.5 | login-log 登录日志：rpcSearchLoginLogs（**用户下拉 + 结果下拉 + 时间范围**，无关键词搜索）+ 无权限降级提示 | B-3 通过：tenant_admin 42501 降级文案 | LoginLogList.vue |
| 5b.6 | audit-log 审计日志：searchAuditLog（关键词/表名/**操作筛选——下拉 = 全部/INSERT/UPDATE/DELETE，已实测（§九 9.4）：operation 为 NULL 的 log_operate 行仅在"全部"可见**）+ 时间线（v_audit_log_timeline） | 列表 + 时间线渲染；筛选行为与值域一致 | AuditLogList.vue |
| 5b.7 | monitor 系统监控：v_system_stats 卡片（**8 项：total_tenants/active_users/total_users/total_roles/total_departments/total_menus/total_apis/stats_time；不展示 online_users/blacklisted**）+ cron 任务/运行历史（超管）+ 租户管理员"仅超管可见"提示 | B-4 通过 | MonitorList.vue |
| 5b.8 | user-center 个人中心：getUserProfile/updateUserProfile（本人免权限点）；**保存成功后同步更新 userStore.userInfo** | 资料回显 + 保存 + 侧栏/头像即时刷新 | UserCenter.vue |
| 5b.9 | dashboard 仪表盘：v_system_stats 卡片修正 | 卡片数据正确 | Dashboard.vue |

**验收（Phase 5 汇总）**：13 页面全部渲染 + CRUD 走通；**tenant_admin 按钮矩阵断言（B-2）**：✅ dept/position/dict CRUD 可见、❌ menu CRUD/role 分配/login-log 隐藏（或降级）、📖 menu/api/role 只读。
**结果项**：`src/views/system/` 全套页面 + 按钮矩阵验证记录

---

## Phase 6：权限指令（§2.6）

**目标**：usePermission + v-perm 指令，按钮显隐与后端权限码一致。

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 6.1 | `usePermission` composable：`hasPerm(code)`——超管短路**仅 'role_super_admin'**；权限码集合来自 v_role_api_detail（role_code ∈ roles）；**模块级内存缓存**（Set<string>，首次拉取，登录/登出清空）；空缓存降级（按钮全隐藏不阻断页面） | B-5 通过：切换账号缓存清空、显隐即时正确 | usePermission.ts |
| 6.2 | `v-perm` 指令实现（挂载时 hasPerm 判断 + 卸载时移除） | 指令可用 | v-perm.ts |
| 6.3 | 页面按钮接入 v-perm（按 §2.5 按钮矩阵配置权限码：sys:dept:create、sys:position:assign、sys:dict:create、sys:menu:create、sys:role-api:bind、sys:profile:update...） | B-1/B-2 通过 | 页面接入 |
| 6.4 | v-auth（路由 meta.authList，页面级）与 v-perm（按钮级）职责说明入 README | 文档说明 | README |

**验收**：§2.7 B-1~B-5 全部通过（testC 全显、tenant_admin 按矩阵、login-log 降级、cron 静默空提示、缓存切换正确）。
**结果项**：usePermission + v-perm + 页面接入 + 权限矩阵验证记录

---

## Phase 7：联调验证（§2.7 全量）

**目标**：A/B/C/D 四组清单全量走查，修复问题直至全绿。

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 7.1 | 认证流 A-1~A-7（testC 登录、新账号注册+JIT、忘记密码、登出、token 静默刷新） | 7/7 通过 | 验证记录 |
| 7.2 | 权限矩阵 B-1~B-5（超管全真、tenant_admin 矩阵、42501 降级、cron 静默空、缓存切换） | 5/5 通过 | 验证记录 |
| 7.3 | 页面数据 C-1~C-5（13 页面、分页 clamp、镜像对照、菜单 CRUD、权限分配父子联动、树渲染） | 5/5 通过 | 验证记录 |
| 7.4 | 边界 D-1~D-3（42501 全局提示、回调 error、空树 fallback） | 3/3 通过 | 验证记录 |
| 7.5 | 修复验证中发现的问题并回归 | 回归 0 新增问题 | 修复记录 |

**验收**：§2.7 清单 20/20 通过；`pnpm run lint` + `pnpm run build` 0 error。验证记录格式：按 A/B/C/D 清单逐项记录（操作步骤 + 结果 + 截图/返回数据），问题项登记修复记录。
**结果项**：联调验证报告（可提交仓库 docs/ 或另存）

---

## Phase 8：收尾发布

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 8.1 | 全量 lint + build（vue-tsc） | 0 error | CI 通过 |
| 8.2 | 提交推送 GitHub（feature/frontend-alignment 分支，每阶段一个 commit 已含） | PR 或分支推送成功 | commit 记录 |
| 8.3 | README 更新：环境变量说明（.env.example）、权限指令用法、Logto Console 配置指引（引用后端文档） | README 完整 | README |
| 8.4 | 遗留项登记：backend 模式切换（§2.4 下一轮）、Pinia 缓存（§2.6 下一轮） | 遗留清单写入 README/文档 | 遗留记录 |

---

## 跨机器协作要点（另一台电脑开发）

1. **仓库**：`git clone` OmniAdmin → 分支 `feature/frontend-alignment`；后端 OmniPG 保持 `feature/logto-authn`（035 已含全部补丁）
2. **后端地址**：开发机通过局域网访问宿主：`VITE_API_BASE=http://<宿主IP>:3100`；Logto：`VITE_LOGTO_ENDPOINT=http://<宿主IP>:3001`（或实际端口）
3. **Logto Console 调整（关键）**：SPA 应用的 redirect URI / post sign-out URI / **CORS 允许来源**都要加入开发机地址（`http://<开发机IP>:5173`）——否则授权回调被拒
4. **测试账号**：testC / tenant_admin 密码由后端提供；新注册账号走 A-4 流程
5. **每阶段结束**：提交 + 在阶段验收表打勾；遇到后端疑问对照 §1.2 权威清单或审查文档 27~32 号

## 依赖关系与风险

- Phase 3 依赖 P-2~P-4（Console 配置）；Phase 5 依赖 035 部署（P-1）；Phase 7 依赖全部前置
- 若宿主 035 未部署：Phase 1/2 可先行（类型/API 层不依赖运行时），联调受阻
- 已知遗留（不阻塞本轮，开发计划见 **Phase 9**）：视图全量可见性（invoker 化）、backend 菜单模式、Pinia 缓存、audit-log p_operation 值域（**已实测**）、user_profile 业务列实测（**含表定义缺失隐患**）

---

## 九、Phase 9：遗留优化计划（下一轮迭代）

> 本轮（Phase 0-8）不阻塞的遗留项，按优先级排序。9.1 为安全项（P1），9.2/9.3 为架构演进，9.4/9.5 为实测落地项（9.4 已实测出结果，可提前并入 Phase 5b.6 顺手完成）。

### 9.1 视图 invoker 化（P1 安全优化——消除个人信息跨租户暴露面）

**目标**：31 个 `api_v1_public` 视图 `security_invoker=true`，让 012/017 的 RLS 策略真正生效（超管全量 / 租户管理员同租户 / 普通用户本人），替代当前"视图 owner 权限全量可见"的既定模型。

**任务**：

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 9.1.1 | **RLS 策略盘点**：31 视图涉及底层表逐个核对策略完备性——012 镜像表（users/tenants/user_tenants ✓ 有策略）、017 自主表（department/dict_type/dict_data/position/user_position/iam_menu/iam_api/iam_role_api/iam_role_menu/app_config）、audit_log、login_log；⚠️ **role 表未 ENABLE RLS——invoker 化后全拒，需补共享读策略 `USING (true)`**；⚠️ login_log.tenant_id 为 NULL 无法简单隔离——需"超管 OR 本租户成员"策略 | 策略核对表 100% 覆盖 | 核对表 |
| 9.1.2 | 补缺策略迁移：role 共享读、login_log 成员过滤、audit_log（超管/本租户）等 | 全部表有明确策略 | 迁移补丁 |
| 9.1.3 | 迁移 036：`ALTER VIEW api_v1_public.xxx SET (security_invoker = true)` × 31（+ 视图验证块：超管全量/租户管理员同租户/普通用户本人断言） | PGlite + 宿主验证通过 | 036 迁移 |
| 9.1.4 | 前端回归：管理页数据范围（B/C 复测）——tenant_admin 用户列表收窄为本租户、普通用户仅本人；页面提示文案（如"仅显示本租户数据"）；角色目录（role 视图）仍全量 | 回归 0 问题 | 回归记录 |

**验收**：实测 `v_user_list`：超管全量 / tenant_admin 同租户 / 普通用户 1 行；`v_user_roles` 同理；role 目录不受影响；管理页功能回归通过。
**结果项**：迁移 036 + 策略核对表 + 前端回归记录

### 9.2 backend 菜单模式（菜单/权限由后端驱动）

**目标**：登录后拉取可访问资源（`get_user_menu()` + `v_role_api_detail`）存入 Pinia userStore，路由动态注册（addRoute）替代本地全量注册——菜单管理页配置即时生效，前端零改动。

**任务**：

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 9.2.1 | userStore 扩展 `resources: { menus: MenuRouteItem[], permissions: Set<string> }` | 状态定义 + 类型 | userStore.ts |
| 9.2.2 | 登录回调/应用启动：拉取 get_user_menu + v_role_api_detail → 写入 userStore（与 usePermission 共用一次请求） | Network 验证登录后仅 2 个资源请求 | 资源拉取逻辑 |
| 9.2.3 | 路由动态化：`VITE_ACCESS_MODE=backend` 时启用 MenuProcessor（两遍构建/button/link 过滤/hideInMenu/component 直用+兜底）+ `router.addRoute`；空树 fallback 本地 asyncRoutes；404 兜底路由 | backend 模式菜单树与 iam_role_menu 绑定一致 | 动态路由代码 |
| 9.2.4 | **path 一致性核对**：本地路由表 vs iam_menu.path（011 种子清单）——不一致项登记并修正（后端菜单 path 或本地路由） | 核对表 0 不一致（或已登记修正） | 核对表 |
| 9.2.5 | 登出清空 resources | 登出后资源清空验证 | 登出逻辑 |

**验收**：backend 模式下：tenant_admin 只见绑定菜单；管理页新增菜单后刷新即时生效（前端零改动）；frontend/backend 两种模式切换验证通过。
**结果项**：userStore 扩展 + 动态路由 + path 核对表

### 9.3 usePermission Pinia 缓存迁移（与 9.2 同批）

**任务**：

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 9.3.1 | usePermission 从模块级内存 Set 迁 `userStore.permissions`（9.2.2 预拉共用，hasPerm 读缓存） | 代码重构 + 引用清理 | usePermission.ts |
| 9.3.2 | 缓存生命周期：登录预拉 / 登出清空 / F5 刷新后首次 hasPerm 重新拉取（可选 pinia-plugin-persistedstate 持久化） | 切换账号、F5 场景按钮显隐正确（B-5 复测） | 生命周期逻辑 |

**验收**：B-5 回归 + F5 刷新场景正常。
**结果项**：usePermission 重构

### 9.4 audit-log p_operation 值域（✅ 已实测，直接落地）

**已实测结果**（源码核实）：`audit_trigger_func.sql` → `p_operation := TG_OP`——operation 列值 = **`INSERT` / `UPDATE` / `DELETE`**（大写）；`log_operate()` 写入的行**不填 operation 列**（NULL）。

**任务**：

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 9.4.1 | audit-log 页筛选器下拉 = 全部 / INSERT / UPDATE / DELETE（operation 为 NULL 的 log_operate 行仅在"全部"可见） | 筛选 INSERT 只出触发器行；全部含操作审计行 | AuditLogList.vue 筛选器 |

**验收**：筛选行为与实测值域一致。💡 可提前并入 Phase 5b.6（不占独立阶段）。
**结果项**：筛选器代码

### 9.5 user_profile 业务列实测（含表定义缺失隐患）

**新发现隐患**：`user_profile` 表在**源码树中无建表定义**（Casdoor 时代遗留，仅宿主库存在）——012 迁移 `ALTER TABLE sys_user_profile` 依赖它，**全量重建（新库）会失败**。

**任务**：

| # | 任务 | 验收标准 | 结果项 |
|:--|:--|:--|:--|
| 9.5.1 | 后端：补 `user_profile` 建表定义（`CREATE TABLE IF NOT EXISTS user_profile` + 权威列清单）到迁移链（036 或 init） | 新库全量重建成功；宿主重放幂等 | 建表迁移 |
| 9.5.2 | 前端：连库实测业务列（`SELECT column_name FROM information_schema.columns WHERE table_name='user_profile'`）→ user-center 表单字段（白名单 = 业务列 − 9 排除列：user_id/tenant_id/dept_id/时间戳/审计列） | 表单字段与实测列一致；tenant_id/dept_id 不可编辑 | 实测记录 + UserCenter 表单 |

**验收**：新环境重建通过；user-center 编辑保存/回显与后端白名单一致（C 回归）。
**结果项**：建表迁移 + 实测记录
