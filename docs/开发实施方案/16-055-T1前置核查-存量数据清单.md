# 16-055-T1 前置核查 — 存量数据清单（菜单权限单表化）

> **创建日期：** 2026-08-12
> **关联：** `docs/开发实施方案/16-菜单权限单表化-SharpFort模型-决策与实施清单.md`（v1.1，T1 前置核查步骤）
> **执行脚本：** `scripts/055-t1-precheck.sql`（只读，WSL Pigsty app_db 执行：`psql -U app_owner -d app_db -f scripts/055-t1-precheck.sql`）
> **数据来源声明：** ⚠️ 本机（Windows 开发机）无 psql / WSL 发行版 / 存量库不可达（192.168.0.128:5432 关闭）——**§2-§4 为迁移链 seed 静态重建的"期望基线"（标注推断项），§5 为存量库实测 SQL（结果待执行后回填）**。043 实测时点事实：api_code IS NULL ≈14 行（043 迁移头注）。

---

## 1. 核查总览（期望 vs 实测）

| 指标 | 静态基线（迁移链推导） | 存量库实测 | 055 处置 |
|---|---|---|---|
| iam_api 总行数 | ≈41-43（011 原始 ≈17-19 + seed 24） | 待实测（§0） | — |
| **有码行** | **≈27**（024×19 + 029×2 + 030×1 + 040 赋码×1 + 042×1 + 043×3） | 待实测（§3） | 按 T1 转换（回填/新建 button 行） |
| **无码行** | ≈14（043 实测）；011 原始 17-19 行 − 040 赋码 1 − 045 删 4 | 待实测（§2） | **D9 直接清除** |
| 死端点行 | /sys_api、/sys_menu、/sys_role、/sys_user、/rpc/approve_role_request、/rpc/kick_user（045 已删）等 | 待实测（§4） | **D9 直接清除**（ALIVE 且无码的按拍板亦清除——彻底重构） |
| button 行数 | 033 回填（UserAdd/Edit/Delete 已由 045 删除，剩余未知） | 待实测（§5） | 转换目标载体 |
| button 行空 api_code | 0（040 验证块断言） | 待实测（§0） | CHECK 保证 |
| role_api 绑定总数 | 055 前未知（039 时点 55 条） | 待实测（§7） | 有码行绑定 → role_menu；无码行绑定随行清除 |
| 一码多行 | 0（023 部分唯一索引保证） | 待实测（§8） | 055 后放开（D4） |

---

## 2. 静态基线 A：有码行完整清单（迁移链 seed，055 转换对象）

> 来源：024/029/030/040/042/043 seed + 035 清理（sys:export、sys:user-role:sync、sys:session:cleanup 已删）。**共 27 行**。api_code 全部形如 `sys:xxx:yyy`（含 ':'，符合 Admin.NET CheckMenuParam 格式约定）。

| # | api_code | path | method | 来源迁移 | 备注 |
|---|---|---|---|---|---|
| 1 | sys:dept:create | /rpc/sys:dept:create | POST | 024 | 部门-新增 |
| 2 | sys:dept:update | /rpc/sys:dept:update | POST | 024 | |
| 3 | sys:dept:delete | /rpc/sys:dept:delete | POST | 024 | |
| 4 | sys:position:list | /rpc/sys:position:list | POST | 024 | 岗位-查询 |
| 5 | sys:position:create | /rpc/sys:position:create | POST | 024 | |
| 6 | sys:position:update | /rpc/sys:position:update | POST | 024 | |
| 7 | sys:position:delete | /rpc/sys:position:delete | POST | 024 | |
| 8 | sys:position:assign | /rpc/sys:position:assign | POST | 024 | 岗位-分配 |
| 9 | sys:dict:create | /rpc/sys:dict:create | POST | 024 | 字典-新增 |
| 10 | sys:dict:update | /rpc/sys:dict:update | POST | 024 | |
| 11 | sys:dict:delete | /rpc/sys:dict:delete | POST | 024 | |
| 12 | sys:menu:create | /rpc/sys:menu:create | POST | 024 | 菜单-新增 |
| 13 | sys:menu:update | /rpc/sys:menu:update | POST | 024 | |
| 14 | sys:menu:delete | /rpc/sys:menu:delete | POST | 024 | |
| 15 | sys:role-api:bind | /rpc/sys:role-api:bind | POST | 024 | **055 后权限点删除（随 rpc_set_role_apis/041 删除）** |
| 16 | sys:role-menu:bind | /rpc/sys:role-menu:bind | POST | 024 | 保留（rpc_set_role_menus 门槛） |
| 17 | sys:profile:update | /rpc/sys:profile:update | POST | 024 | 用户资料-修改 |
| 18 | sys:tenant:list | /rpc/sys:tenant:list | POST | 024 | 租户-查询 |
| 19 | sys:tenant-member:list | /rpc/sys:tenant-member:list | POST | 024 | 租户成员-查询 |
| 20 | sys:config:write | /rpc/sys:config:write | POST | 029 | 配置-写入 |
| 21 | sys:import | /rpc/sys:import | POST | 029 | 数据导入 |
| 22 | sys:login-log:list | /rpc/sys:login-log:list | POST | 030 | 登录日志-查询 |
| 23 | **sys:user:list** | **/sys_user** | **GET** | 040（011 行赋码） | 用户列表展示；**唯一非 /rpc/ 路径的有码行** |
| 24 | sys:data-scope:bind | /rpc/sys:data-scope:bind | POST | 042 | 数据范围-绑定 |
| 25 | sys:api:create | /rpc/sys:api:create | POST | 043 | **055 后权限点删除（随 043 RPC 删除）** |
| 26 | sys:api:update | /rpc/sys:api:update | POST | 043 | **同上** |
| 27 | sys:api:delete | /rpc/sys:api:delete | POST | 043 | **同上** |

**055 后处置：**
- #15 sys:role-api:bind、#25-27 sys:api:* → **权限点删除**（随 RPC 删除，含 role_super_admin 绑定）
- #23 sys:user:list（GET /sys_user）→ **回填到同码 button 行**（若 UserList 下存在）；此码对应"一码多端点"首个实例：若 /rpc/search_users 等用户查询端点后续挂此码 → 新建同码 button 行（D4）
- 其余 22 个 /rpc/sys:* → 无同码 button 行（024/029/030/042/043 seed 时**未创建按钮**）→ **按 T1 规则 2 新建 button 行**（parent_id 按 api_group 归属映射：部门管理/岗位管理/字典管理/菜单管理/角色管理/系统配置/用户资料/日志监控/数据导入/租户管理/API管理 目录）

---

## 3. 静态基线 B：无码行推断（011 原始，D9 清除对象）

> 011 从旧 sys_api 目录迁移（023 才加 api_code 列，从未赋码）。039 回填统计（41 行分组）反推 011 原始 ≈17-19 行，path 模式按 039 回填规则 + 040/045 处置推断。**具体 path 值待存量库实测（§2 SQL）**。

| 分组（039） | 推断行 | 死端点判定 | 处置 |
|---|---|---|---|
| 菜单管理（011 部分 ≈4 行） | /sys_menu + 3 行 /rpc/sys:menu:* | /sys_menu = **DEAD**（实际暴露 /iam_menu） | 清除 |
| API管理（≈4 行） | /sys_api + 3 行 | /sys_api = **DEAD**（实际暴露 /iam_api） | 清除 |
| 角色管理（011 部分 ≈4 行） | /sys_role + 3 行 /rpc/sys:role* | /sys_role = **DEAD**（实际暴露 /role） | 清除 |
| 用户管理（≈4 行） | /sys_user（GET 已赋码 #23；POST/PATCH/DELETE 045 已删）+ 3 行 | /sys_user = **DEAD**（实际暴露 /users） | 清除（GET 行转为 #23 button） |
| 系统基础（3 行） | /rpc/get_user_menu、/rpc/approve_role_request、/rpc/kick_user | get_user_menu = **ALIVE**；approve_role_request = **DEAD**（015 删）；kick_user = **DEAD**（015 删、045 删行） | 按拍板全清（含 ALIVE 的 get_user_menu——无码行不保留） |

**⚠️ 提醒**：若存量库实测发现**无码但 ALIVE 的端点行**（如 /rpc/search_users、/rpc/get_role_users 等查询类 RPC），按 D9 拍板同样清除——055 后由前端菜单管理按需重建（新建 button 行 + 赋码 + 挂端点）。清除前请以 §2 实测清单为准核对一遍。

---

## 4. 静态基线 C：button 行挂接核查

| 核查项 | 静态推断 | 实测 SQL |
|---|---|---|
| button 行总数 | 033 回填（动作词后缀：Add/Edit/Delete/Query/Export/Import/Reset/Assign/Status/Remove/Detail/Pwd）；045 删 UserAdd/UserEdit/UserDelete；**存量未知** | §5 |
| button 行 api_code 空 | 0（040 验证块断言 v_btn_nulls=0） | §0 |
| 每 button 挂接 api 行数 | 044 仅 3 个用户按钮 1:1（已删）；其余 button 无挂接先例 → 预期全部 0 或 1，**>1 即冲突清单** | §5 |
| 一码多行（iam_api 侧） | 0（023 idx_iam_api_code 部分唯一索引） | §8 |
| 转换映射预期 | §10 SQL：有码行 ↔ 同码 button 行配对结果 | §10 |

---

## 5. 存量库实测 SQL（在 WSL Pigsty 执行）

完整脚本：`scripts/055-t1-precheck.sql`（§0-§11，全部只读）。执行方式：

```bash
# WSL Pigsty 宿主
psql -U app_owner -d app_db -f /path/to/scripts/055-t1-precheck.sql
```

**实测结果记录表（执行后回填）：**

| 指标 | 实测值 | 与静态基线差异 | 备注 |
|---|---|---|---|
| api_total / with_code / no_code / orphan | / / / | | |
| button 行数 / 空码按钮 | / | | |
| role_api 绑定总数 / 无码行上绑定 | / | | |
| 死端点行数（DEAD） | | | |
| ALIVE 且无码行数 | | | 按 D9 清除，列出清单供确认 |
| 一码多行冲突 | | | |
| button 挂接 >1 冲突 | | | |
| 无同码 button 的有码行数 | | | 需新建 button 行数 |

---

## 6. 核查规则与判定标准（055 实施依据）

1. **死端点判定**：path 不在 `api_v1_public` 暴露清单（13 个 rpc 源文件 + 21 个视图）→ DEAD。⚠️ `docs/开发实施方案/API速查表.md` 已过时（含 user_login_sso/health_check/create_user/kick_user 等已删端点），**不可作为暴露依据**；以 api_v1/public 目录 + 存量库 information_schema/pg_proc 实测为准（§4 SQL 自动判定）
2. **无码行**：api_code IS NULL → 按 D9 清除（不转换、不赋码）；其 role_api 绑定随行清除（超管短路兜底：role_super_admin 全部绑定丢失无实际影响；tenant_admin 绑定全在有码行）
3. **有码行转换**：同码 button 行存在 → 回填 api_url/api_method（成对，method 值域含 '*'）；不存在 → 按 api_group/menu_id 归属新建 button 行（menu_name=api.name、描述→remark、parent_id=归属目录）；**一码多行逐行映射不合并**
4. **授权转换**：iam_role_api 有码行绑定 → iam_role_menu 对应 button 行（逐行）；role_super_admin 绑定随行转换（或依赖超管短路不转换——055 实施时拍板，默认转换保持数据完整）
5. **055 后 button 行必带码**（040 CHECK）+ router/component 置空（D8 CHECK）+ api_method 成对值域（D6 CHECK）

---

## 7. 待办

- [ ] 用户在 WSL Pigsty 执行 `scripts/055-t1-precheck.sql`，回填 §5 结果表
- [ ] 核对无码 ALIVE 端点行清单（§4 实测），确认 D9 清除无异议
- [ ] T1 冻结 → T2 055 迁移编写
