# 分支策略与发布流程

本文记录 OmniPG 分支收敛结果（2026-08-18 完成）与日常开发分支流程、master 保护与 CI 要求、版本发布流程。

## 1. 分支收敛结果（2026-08-18 完成）

**现状**：master 为唯一长期分支（origin/HEAD → origin/master），已包含 feature/logto-authn 全部代码与 wiki 编写成果（原 docs/wiki-rewrite 分支内容完整并入）。

| 分支 | 处置 |
| --- | --- |
| master | ✅ 唯一长期分支，当前主线 |
| docs/wiki-rewrite | ✅ 已并入 master 后删除（内容完整保留） |
| feature/logto-authn | ✅ 已打归档 tag 后删除 |
| dev / feature/casdoor-managed-roles / fix/cicd-v2-deployment-issues / refactor/cicd-v2 | ✅ 已打归档 tag 后删除 |

归档 tag（只读，保留历史）：archive/wiki-rewrite-final、archive/logto-authn-final、archive/dev-final、archive/casdoor-managed-roles-final、archive/fix-cicd-v2-final、archive/refactor-cicd-v2-final。

其他历史数据保留：

- v0.1.0 tag：迁移基线 squash 0.1，历史 62 个迁移存档于此（git show v0.1.0 -- db/migrations/public/）。
- docs/ 历史文档：wiki 完成后已归档清理，内容保留在 git 历史（git log --all -- docs/ 可追溯）。

## 2. 日常开发流程：短生命周期分支 + PR

- 分支命名：feature/<功能>、hotfix/<问题>、docs/<主题>、fix/<问题>。
- 流程：从 master 拉分支 → 开发 + 本地验证（make test）→ PR 到 master → CI 状态检查通过 → review → merge → 删除分支。
- 部署不依赖分支名：CI 为 PR 触发（.github/workflows/ci.yml，路径过滤）；部署 workflow（deploy-db / deploy-gateway / deploy-infra / deploy-all）为 workflow_dispatch 手动触发，按 environment（staging/production）区分。

## 3. master 保护与 CI 要求

- CI 路径过滤（ci.yml）：

| 变更路径 | 检查 |
| --- | --- |
| db/migrations/**、db/src/**、db/api_v1/**、db/init/**、db/tests/** | SQL Lint（sqlfluff，容错）+ dbmate up --dry-run（postgres:18 服务） |
| gateway/** | docker compose config --quiet + docker compose build |
| db/syncer/** | Go build + go test（syncer 已退役，检查保留） |
| infra/** | yamllint（infra/pigsty.yml 唯一配置） |

- Go syncer 已退役：ci.yml 的 syncer-check 作业与 deploy-gateway.sh 的 docker compose build syncer 段为历史遗留（检查保留，不影响部署）。
- 本地双闸：verify-fresh-db.sh（冷启动结构比对 + pgTAP）+ make test（pgTAP + e2e-test.sh，Logto 版）。
- 建议保护规则：master 禁止 force push 与直接推送；PR 至少 1 个 review；状态检查（CI）必须通过；environment 规则（staging/production）用于部署 workflow。

## 4. 版本发布流程

1. **冻结**：连续观察期无新迁移。
2. **质量闸**：make test 全绿 + verify-fresh-db.sh 通过 + 迁移幂等两遍重放不炸。
3. **迁移治理（如需）**：发布时 squash 一次（历史迁移 → 新 baseline），旧迁移以 tag 留存（参考历史 18 号文档 SOP：pg_dump 反写 → 幂等三件套 → 账本收敛 → 冷启动验证 → git tag；文档已归档，git 历史可查）。
4. **打 tag**：git tag vX.Y.Z && git push origin vX.Y.Z；tag 内保留被 squash 的历史。
5. **Changelog**：按域列出（db 迁移/源码、gateway 配置、infra、脚本、文档），并记录备份情况（见 [备份与恢复](备份与恢复.md)）。
6. **部署**：deploy-infra → deploy-db → deploy-gateway → init-apisix-routes.sh（Logto 版）→ e2e 验收；生产环境经 workflow_dispatch + environment 审批。

## 5. 归档策略：archive/* tag 保留历史

- 归档 tag 命名：archive/<分支名>-final。
- 归档 tag 只读、不参与日常开发；需要追溯旧实现时从 tag checkout 只读查看。
- 删除分支前必须确认：目标分支的全部提交已存在于长期分支（master）或归档 tag 中。

---

> 参考：[项目主页](../首页.md) · [测试总览](../07-测试/测试体系总览.md) · [部署指南总览](../03-部署指南/部署总览.md) · [迁移指南](../05-开发指南/数据库迁移.md) · [备份与恢复](备份与恢复.md)
